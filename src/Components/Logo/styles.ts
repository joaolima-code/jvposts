import styled from 'styled-components';

export const Container = styled.div`
  a {
    color: var(--primary-color);

    font-size: 2.5rem;
    font-weight: 700;
    line-height: 3.5rem;

    strong {
      color: var(--title);
    }
  }
`;
